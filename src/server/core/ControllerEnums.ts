/**
 * Enumeration for the different action filters
 */
export enum ActionType {
    DELETE,
    GET,
    PATCH,
    POST,
    PUT,
}

/**
 * Enumeration with the different view types
 */
export enum ViewType {
    VIEW,
    RAW,
    JSON,
}
